function simpleArraySum(ar) {
    function simpleArraySum(ar) {
        var[] num = [1, 2, 3];
        for(var c; c < num.length; c++){
           var soma = num[0] + num[1] + num[2];
        }
        return soma;
     }
     
     simpleArraySum();
}